## Arroyo San Antonio - Estructura escalonada (2018-04-25)
:camera: Fotos: rcfdtools<br>Categoría: Technical field visit

```geojson
{
  "type": "Feature",
  "geometry": {
    "type": "Point", 
    "coordinates": [-73.469932, 9.517357]
  }, 
  "properties": {
    "Name": "Arroyo San Antonio - Estructura escalonada"
  }
}
```

`File` **16/20180425_152015.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:20:15 `Aperture` Not known `Brightness` 9.52 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0002808988764044944 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152015.jpg)

`File` **16/20180425_152018.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:20:18 `Aperture` Not known `Brightness` 9.51 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0002860411899313501 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152018.jpg)

`File` **16/20180425_152022.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:20:22 `Aperture` Not known `Brightness` 9.54 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0002765486725663717 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152022.jpg)

`File` **16/20180425_152024.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:20:24 `Aperture` Not known `Brightness` 9.54 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0002962085308056872 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152024.jpg)

`File` **16/20180425_152031.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:20:31 `Aperture` Not known `Brightness` Not known `Color space` 1 `Compression` Not known `Exif version` 0220 `Exposure mode` 0 `Exposure time` Not known `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152031.jpg)

`File` **16/20180425_152216.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:22:16 `Aperture` Not known `Brightness` 8.69 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0005263157894736842 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152216.jpg)

`File` **16/20180425_152526.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:25:26 `Aperture` Not known `Brightness` 9.1 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.00039936102236421724 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152526.jpg)

`File` **16/20180425_152531.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:25:31 `Aperture` Not known `Brightness` Not known `Color space` 1 `Compression` Not known `Exif version` 0220 `Exposure mode` 0 `Exposure time` Not known `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152531.jpg)

`File` **16/20180425_152539.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:25:39 `Aperture` Not known `Brightness` 9.16 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.0003654970760233918 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152539.jpg)

`File` **16/20180425_152629.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:26:28 `Aperture` Not known `Brightness` Not known `Color space` 1 `Compression` Not known `Exif version` 0220 `Exposure mode` 0 `Exposure time` Not known `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152629.jpg)

`File` **16/20180425_152719.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:27:19 `Aperture` Not known `Brightness` 9.1 `Color space` 1 `Compression` 6 `Exif version` 0220 `Exposure mode` 0 `Exposure time` 0.000390625 `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152719.jpg)

`File` **16/20180425_152723.jpg** <sub>`OS version` G930PVPS5BRA1 `Date` 2018:04:25 15:27:23 `Aperture` Not known `Brightness` Not known `Color space` 1 `Compression` Not known `Exif version` 0220 `Exposure mode` 0 `Exposure time` Not known `Focal length` 4.2 `Lens model` Not known `Lens specification` Not known `Orientation` 1 `Scene type` Not known `Software` G930PVPS5BRA1 `White balance` 0</sub>
![GISMobile.POI](20180425_152723.jpg)

| [:house: Inicio](../Readme.md) |
|---|